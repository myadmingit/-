server: 
  port: 9009
spring: 
  application:  
    name: tansquare-user #指定服务名
  datasource:  
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/tensquare_user?characterEncoding=UTF8
    username: root
    password: root
  jpa: 
    database: MySQL
    show-sql: true
  rabbitmq:
    host: localhost
  redis:
    host: localhost
jwt:
  config:
    key: itcast
    ttl: 3600000